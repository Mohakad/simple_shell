#include "shell.h"
void execute(char *com, char **args)
{
	int status;

	pid_t pid = fork();
  if (pid == 0) {
    if (execve(com, args, NULL) == -1) {
      perror(com);
      exit(1);
    }
  } else if (pid > 0) {
    wait(&status);
  } else {
    perror("fork");
    exit(1);
  }
}
